LENS: Cross-surface consistency and functional completeness â€” do web and touch share one IA (same spine: names, order, icons, status semantics, nav pattern), and is every current feature explicitly placed (including the real surface asymmetries: C6 OTA touch-only, P4 /update web-only, and web-only download/RINEX-export/full-logs/CA-cert/setup)?
  best=P3 (Status-dashboard)
  rankings=P3 (Status-dashboard):9, P2 (Live-instrument MFD):8, P1 (Task-flow):6
  mustKeep=1) ONE shared five-section spine with identical names, order, icons, and status-color semantics on web and touch, with the web 720px 2-column breakpoint being literally the touch layout (P3/P2 both express this; P3's five sections match today's five touch tabs at ui.cpp:345-349, so adopt FIVE top-level sections, not six â€” this avoids squeezing the 720px touch tab bar and is the single most important cross-surface decision). 2) Map onto the operator's existing subsystem vocabulary (Position, Links/NTRIP, Storage, System) NOT a renamed lifecycle â€” keep operator/device terms (NTRIP, RINEX) so the spine matches mental model and reduces cross-section duplication. 3) A global worst-of-all-subsystems status pill in the header on BOTH surfaces (marine shared-alarm-strip) + day/night toggle in header, persisted in Storage so both surfaces agree (net-new â€” verified no existing theme system). 4) Explicit, honest placement of the REAL asymmetries in identical IA slots: C6 OTA (touch-only today) and P4 /update OTA (web-only today) both live in System; web-only download / RINEX-export / full-log-viewer / CA-cert / first-run /setup named in their owning section as web-only â€” never hidden (all three do this; it is mandatory for completeness). 5) A single shared CSS :root/[data-theme] token block emitted once, replacing the three divergent inline web themes (verified 42 scattered color/style occurrences in web_server.cpp) â€” required for true cross-surface visual consistency. 6) Drill-down deep-linking: every Dashboard/Helm tile is also a shortcut into its owning section, and any control duplicated across surfaces or screens (NTRIP master, RINEX toggle, survey start/confirm) must read the same single status() snapshot and route through one backend call with the resync-guard pattern so web and touch cannot drift. 7) The net-new recursive/bulk SD delete with protected-path guard (never /sdcard, /sdcard/logs, /sdcard/rawdata as dirs; refuse the active RINEX file), off-task worker execution with atomic progress, exposed identically on both surfaces â€” verified absent today (sd_manager.cpp:230 is single-entry only).

LENS: Embedded feasibility â€” LVGL on a 720x720 ST7703 panel + esp_http_server chunked HTML, judged against the ACTUAL codebase: ~95KB free internal SRAM at boot (documented near-fatal constraint; ota25-31 crash-looped on SRAM exhaustion), framebuffer + LVGL heap in PSRAM, a 720x50 partial single draw buffer in internal DMA SRAM, only lv_font_montserrat_14 compiled, ARC/CANVAS/CHART/TABVIEW/KEYBOARD already enabled, web pages built as std::string in heap and emitted via 1024-byte httpd_resp_send_chunk with one shared <style> block in send_page (kAfterTitle), and 29/32 URI handlers registered (3 free). SD is on slow contended SDSPI (esp_hosted owns SDMMC), and delete_entry only does rmdir/unlink so recursive delete is genuinely net-new.
  best=P2 (Live-instrument MFD) â€” narrowly over P3. Both are clearly more feasible than P1, and they are close (8 each), but P2 edges it on two embedded-specific points. First, redraw/SRAM cost: P2 and P3 both correctly isolate the heaviest widget (the sky-plot lv_canvas) inside a single drill-down tab so it is lazy-built and only redrawn when visible â€” the right move against the 720x50 partial single draw buffer and the documented ~95KB internal-SRAM ceiling â€” whereas P1 piles three live arcs plus a canvas plus sat lists onto one Survey screen. Second, between P2 and P3, P2's home screen is meaningfully lighter to render and less duplicated: its Helm tiles are strictly read-only deep-links (one render path, fewer live LVGL objects to mutate under bsp_display_lock each tick), while P3's Dashboard packs seven rollups plus three live inline toggles that each need resync guards and a gated disabled state, raising both per-tick redraw work and drift risk. P3 wins the narrow nav point (five tabs vs P2's six, a smaller change to the existing lv_tabview), so if the team weights the bottom-bar tab budget above home-screen render cost, P3 is the equally-defensible pick. P1 is the least feasible of the three on this lens, almost entirely because of the six-tab bar and the overloaded Survey-in redraw surface, not because of any backend difference (the recursive-delete risk is shared by all three).
  rankings=P1 (Task-flow):6, P2 (Live-instrument MFD):8, P3 (Status-dashboard):8
  mustKeep=1) Enable montserrat_18/28/48 in lv_conf and verify the build â€” these cost flash .rodata, NOT the scarce internal SRAM, so they are affordable and they close the real no-large-numeric gap (only montserrat_14 compiles today). 2) Reuse the EXISTING threading contract verbatim for every new blocking op (recursive SD delete, WiFi, C6/P4 OTA): xTaskCreate worker + std::atomic progress polled by the 1000ms lv_timer, all LVGL mutation under bsp_display_lock â€” this is the c6_ota_progress_ pattern already in ui.cpp and the only proven-safe model. 3) Lazy-build all tiles/modals/file-lists per tab and keep the sat list capped â€” this is the single most important discipline given the documented ota25-31 SRAM crash-loop history and the ~95KB internal-SRAM ceiling at boot. 4) Isolate the sky-plot lv_canvas inside one drill-down tab (Position) so it is only built and redrawn when visible â€” do NOT stack it with live arcs on the same screen, because the draw buffer is a 720x50 partial single buffer in internal DMA SRAM (full screens render in ~15 strips). 5) Reuse already-enabled lv_arc / lv_canvas / lv_chart / lv_tabview / lv_keyboard â€” no new lv_conf widget flags needed; arcs/bars must be set without per-tick LV_ANIM and refreshed at 1Hz only. 6) Cache load_position() in RAM instead of re-reading NVS every refresh tick. 7) Web: emit ONE :root / [data-theme=night] CSS-variable token block exactly once inside the existing kAfterTitle shared <style> in send_page, and retire the divergent private inline styles on /files, /rinex/export, and /logs (keep monospace only on /logs). 8) Respect the established web transport limits as-is: build pages as std::string and emit via 1024-byte httpd_resp_send_chunk, keep Cache-Control:no-store and the 15s/10s/5s poll cadences, and use inline-SVG stroke-dasharray arcs themed by currentColor (no canvas, no JS libraries) so they chunk cheaply. 9) Treat the URI-handler cap as a hard ceiling: 29 of 32 registered today, only ~3 free, and each GET+POST pair burns 2 â€” budget batch-delete + any C6-OTA-web-parity + day/night-persist against those 3 slots and raise max_uri_handlers deliberately if exceeded. 10) Recursive/bulk SD delete is genuinely net-new (delete_entry only rmdir/unlinks, fails on non-empty dirs) and runs over slow WiFi-contended SDSPI â€” it MUST be off-task with progress, a protected-path guard (never the mount root, managed dirs, or the active RINEX file), and is the largest shared engineering risk for ALL proposals. 11) Persist the day/night choice in Storage so the touch Theme struct and the web data-theme stay in agreement, applied via a shared apply_theme() on the LVGL side.

LENS: Marine-instrument aesthetic fidelity & sunlight readability (glanceability, big readouts, status colors, day/night)
  best=P2 (Live-instrument MFD)
  rankings=P2 (Live-instrument MFD):9, P3 (Status-dashboard):8, P1 (Task-flow):6
  mustKeep=1) Persistent global status pill = worst-of-all-subsystems (the marine shared-alarm strip), green LIVE / amber SURVEYING / red ALARM, present on every screen on both surfaces. 2) Redundant status encoding: four colors only, each ALWAYS paired with a word AND a glyph/dot in a capsule pill - mandatory for sunlight glare and color-blindness. 3) Value-carries-color (B&G pattern), not the whole tile, PLUS a 2px crit left-edge bar as a third redundant cue; 'off/idle' is dim, never red. 4) Enable the large numeric fonts the build lacks today (montserrat_18/28/48 - current code compiles only montserrat_14) and use tabular-nums on web so live values don't jitter; exactly ONE hero value per screen. 5) RTCM output rate as the heartbeat hero of a working base, with status-tinted value. 6) Rank tiles so the top-N most-watched (RTCM, Mode, Sats, NTRIP) never scroll out of glance range above the fold (P2's discipline - apply it to whichever IA wins). 7) Survey-in shown against all THREE completion gates (>=300s, >=5 blocks, stability <=0.50m) with an arc, replacing today's misleading time-only bar. 8) Single day/night header toggle, NIGHT default, one shared Theme struct (touch) / one :root + data-theme token block emitted once (web), persisted in Storage so both surfaces always agree - this also kills the three divergent inline web themes that exist today. 9) Arcs (lv_arc / SVG stroke-dasharray themed via currentColor) for all bounded quantities: survey stability, disk %, RSSI, free heap. 10) The 720px 2-column web breakpoint must be visually identical to the touch face so the touchscreen IS 'the web app at its breakpoint'. 11) Mode tile color-coded SURVEY (amber) vs BASE TX (green) as a constant, unambiguous state readout.
